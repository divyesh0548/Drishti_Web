Master grouping upload still takes statement-area from "data/master-grouping.json" file. So only excel file is not enough.

In mapping each GL number is mapped with Grouping and Sub-grouping. Grouping details comes from master grouping file. And sub-grouping comes from "src\lib\ledger-groupings.ts" (subgroupCatalog).

Sub Grouping is matched based matching keywords from catalog and GL description. If not match then first option is picked from catalog of the groupkey.
For example : 
  { key: "borrowings-term-loans", groupKey: "borrowings", label: "Term loans", noteNumber: "5", noteTitle: noteTitleByNumber["5"], statementArea: "balance-sheet", bucketOverride: "non-current-liabilities", keywords: ["term loan", "tl_", "vehicle loan"] },

Note numbers are linked with subgrouping not grouping. in the "src\lib\ledger-groupings.ts" (subgroupCatalog).


Ask : 

Changing note for BS/PL do not affect the extracted report. Because it just updated the values in the provide report format. Changing notes will only change the total amount in statements page.

Notes 28 to 40 are not defined in program, they are just kept as same from source format file.

Will notes be same for all companies?

SubGrouping is missing for LTEL company.




Connection from TB sheet to Financial report generation

1. Master grouping upload with GL codes and their Grouping

2. Trial Balance sheet upload with GL Code, GL description, Amount of Current/Previous year.

3. Based on GL code the group is identified and based on GL description subgroup is identified based on keywords. This mapping is hard coded in program.

4. In the Previous step program also gets Note number from selected SubGroup catalog.

